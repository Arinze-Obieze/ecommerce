const { createClient } = require('@supabase/supabase-js'); require('dotenv').config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SECRET_SERVICE_ROLE_KEY); async function check() { const { data, error } = await supabase.rpc('checkout_transaction', { p_user_id: 'test', p_items: [], p_total: 0 }); console.log('RPC check:', error ? error.message : 'Exists'); } check();
